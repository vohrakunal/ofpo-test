import { MoveBackgroundDirective } from './move-background.directive';

describe('MoveBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
